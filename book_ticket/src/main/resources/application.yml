spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movieTicket
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: s3cr3tKey!@#987_JWT_T0ken$Gen2025*Secure
  expiration: 3600000
